# Global Memory（全局可复用经验）

仅收录**跨多个项目验证过**的经验（`validated_projects >= 3` 且跨类型）。

- 格式与晋升门槛见 `../晋升机制.md`。
- 新增须经晋升门槛，**不得由单次问题直接写入**。
- 本目录与 `genre/`、`project/`、`rejected/` 互不污染；Global 经验被新模型解决或误报率高时，移入 `rejected/` 并标注 reason。
