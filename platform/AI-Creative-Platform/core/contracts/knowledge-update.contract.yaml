# Knowledge Update Contract（NKB 更新契约）
contract:
  id: nkb.update
  version: "1.0.0"
input:
  required:
    - project_id
    - event_id
    - source_chapter
    - approved_build
    - fact_type
    - before
    - after
    - evidence
    - role
output:
  required:
    - event_id
    - nkb_version_after
    - derived_updated
