schema_version: "1.3.0"
# NKB schema 扩展（模板级附加组件）
# add_fields：在基础 11 组件之外，脚手架时为该类型附加的 NKB 组件名（dict 键）。
# 玄幻模板暂不加额外组件；如未来需要（如 Cultivation 修炼体系表），在此追加：
#   add_fields:
#     Cultivation: 修炼体系表
add_fields: {}
