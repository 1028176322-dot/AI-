# 道法百年（novel-dsf）全面治理 · 重构清单

- 执行日期：2026-07-27
- 工具：`platform doctor --quick`（权威健康度）；读-only 探查 + 文件组织比对；受控任务文件归位（mv）。
- 范围：项目全量结构 / 配置 / 命名 / 文件组织，对照 `platform.yaml`、`project.yaml`、`README.md`、`AGENTS.md`、`registry/versions.yaml` 及本项目 #231 治理惯例。
- 方法：先诊断（只读），再对「零风险组织类」立即执行，对「设计/破坏性」类列入清单待授权（遵循 #231「待你授权后再执行」）。
- 平台健康度：`doctor --quick` = 平台级 7 项全 PASS、健康分 100；项目兼容性（platform/contracts/template/plugin）全 PASS；NKB schema 1.2.0（legacy，当前 1.3.0）；内容深度体检被 QuickMode 跳过（WARN）。

---

## 一、治理发现总表（8 项）

| # | 类别 | 发现 | 风险 | 状态 |
|---|------|------|------|------|
| F1 | 布局规范 | 缺 `PROJECT_LAYOUT.yaml`，旧项目未迁移严格目录 | 中 | 待授权 |
| F2 | 配置/路径 | 6 个卷目录散落根，`paths.chapters: ./txt` 与 canonical 真实落点不一致 | 中/低 | 待授权 |
| F3 | 唯一事实源 | `txt/` 导出稿与 md 权威源漂移（SyncGov 债 #223，160/160） | 中 | 待授权+平台工具 |
| F4 | 任务系统 | 根目录游离任务文件（1 重复 + 1 真游离） | 低 | **已执行** |
| F5 | 版本 | NKB schema 1.2.0 滞后（平台当前 1.3.0） | 低 | 保持 legacy，待授权迁移 |
| F6 | 文档冗余 | 平台 Core 文档疑似泄漏到项目根 | 中 | 待授权+逐份比对 |
| F7 | 文件组织(软) | 卷目录内正文与评审产物混放，manifest 误登记评分卡为 published | 低 | 软建议，待授权 |
| F8 | 大纲一致性 | 总纲 vs 卷纲可能漂移 | 低 | 核查，软建议 |

---

## 二、逐项详情（修改前问题 → 修改后方案 → 平台规则依据）

### ✅ F4 根目录游离任务文件（已执行，零风险）
- **修改前问题**：项目根存在 `task_pilot_ch001_fix.yaml`、`task_fix_ch2_20_review.yaml` 两个 Task Packet，未归入 `tasks/<status>/<id>/` 树。经核对：
  - `task_pilot_ch001_fix.yaml`（ID `TASK-FIX-CH001-20260725`）**已在 `tasks/completed/` 存在 3 份**（含 -CHAPTER-REVIEW / -PUBLISH），根副本为**已完成任务的重复件**；
  - `task_fix_ch2_20_review.yaml`（ID `TASK-FIX-CH2-20-REVIEW-20260725`）**不在 tasks 树中**，为**真正游离件**。
  - 违反 `task_system.enforcement_mode: strict`（无 Task ID 不执行、任务须落在 tasks 状态树）。
- **修改后方案（已落地）**：
  - 重复件 → `tasks/archive/task_pilot_ch001_fix.yaml`（历史归档，可逆保留）；
  - 真游离件 → `tasks/claimed/task_fix_ch2_20_review.yaml`（恢复可追踪，未断言完成）；
  - 根目录 `task_*.yaml` 已清空。纯位置调整，内容不变。
- **平台规则依据**：`project.yaml` `task_system.enforcement_mode: strict` + `task_required_for`；`README.md` §九 状态机 `Planned→Draft→Reviewing→Fixing→Regression→Approved→Published`；任务须由 tasks 树管理。
- **执行状态**：✅ 已执行（2026-07-27，mv，可逆）。

### ⏳ F1 缺 `PROJECT_LAYOUT.yaml`（待授权）
- **修改前问题**：项目根无 `PROJECT_LAYOUT.yaml`。`README.md` §九 明确「新项目自动生成 `PROJECT_LAYOUT.yaml` 并启用严格目录；**旧项目不自动迁移**」。本项目为 legacy，未迁移，导致文件组织缺乏权威规范，间接催生 F2/F4 等散落问题。
- **修改后方案**：二选一，待你裁定——
  - (a) 为项目生成 `PROJECT_LAYOUT.yaml`（依据 `registry/versions.yaml` `project_layout: 2.0.0`），显式声明各目录职责与允许/禁止项；
  - (b) 在 `project.yaml` 或布局文件显式声明本项目为 **legacy 豁免**，记录已知偏差。
- **平台规则依据**：`README.md` §九「自主学习与严格新项目」；`registry/versions.yaml` `project_layout: 2.0.0`。
- **风险**：低（生成/声明本身无破坏；据此重排目录需另议）。
- **执行状态**：⏳ 待授权。

### ⏳ F2 `paths.chapters` 与 canonical 真实落点不一致（待授权）
- **修改前问题**：`canonical_manifest.yaml` 引用路径为 `第一卷_道生/...`、`第二卷_京华/...`（相对项目根，共 308+265 条），即 **canonical 正文（`.md`）真实落点在项目根 6 个卷目录**；而 `project.yaml` 声明 `paths.chapters: ./txt`（指向 `.txt` 导出稿）。`paths.*` 中**未显式声明根卷为 canonical 落点**，声明位置与真实 canonical 位置不符；6 个卷目录裸露根目录，破坏根整洁与可移植性。
- **修改后方案**（二选一，待你裁定）：
  - (a) **推荐（低风险提示）**：在 `project.yaml` 增设 `paths.chapters_canonical: ./`（或 `./canonical`），保留 `paths.chapters: ./txt` 为导出路径，澄清语义；
  - (b) **不推荐（高风险）**：将 6 个卷目录整体移入 `txt/`（或新建 `canonical/`），并同步改写 `canonical_manifest.yaml` 全部 573 条 path 与重算 hash——破坏性大、易断链。
- **平台规则依据**：`project.yaml` `paths.*` 章节；`canonical_manifest.yaml` 真相源约定（generated_by: publish_service）；`README.md` 三层隔离（Project 私有正文/大纲属 Project 层，路径须声明）。
- **风险**：(a) 低；(b) 高（需重建 manifest、重算 hash、可能断链）。
- **执行状态**：⏳ 待授权（建议采用方案 a）。

### ⏳ F3 `txt/` 导出稿与 md 权威源漂移（待授权 + 平台工具）
- **修改前问题**：`txt/` 为 `.txt` 导出稿；评审卡（`第一卷_道生/审读_*.md`）反复明示「**md 源为权威，txt 为滞后导出物，已检测 160/160 漂移**」（前生↔前世等同义漂移、标题格式差异、部分 txt 未导出）。`txt/` 与 canonical `.md` 双份存在且不一致，违背「唯一事实源」。
- **修改后方案**：**不手动删改**。应通过平台同步机制（SyncGov / 平台导出同步命令）从 md 重新生成 `txt/`，消除漂移；或明确 `txt/` 生命周期由工具管理、不人工维护。
- **平台规则依据**：`canonical_manifest.yaml` `source: legacy-pre-manifest` + 评审卡 P3「审查统一以 md 为唯一源」；平台「NKB 唯一事实源 / 编辑≠发布」原则。
- **风险**：手动删 `txt/` 不影响 md 权威但丢失机检导出物；重生成需平台工具。
- **执行状态**：⏳ 待授权 + 需平台同步工具（关联 SyncGov 债 #223）。

### ⏳ F5 NKB schema 1.2.0 滞后（保持 legacy，待授权迁移）
- **修改前问题**：`doctor` 报告 NKB schema 1.2.0（requires `>=1.2.0` → PASS），但 `registry/versions.yaml` 当前 `nkb_schema: 1.3.0`。属 legacy schema 漂移（与 #231 NKB legacy 校验一致：缺 Locations/Organizations 组件、中文状态枚举 vs 新英文枚举）。
- **修改后方案**：维持 legacy（#231 已裁定不强制迁移）；如需消除告警，须单独授权后按 NKB 1.3.0 迁移。当前不影响正文运行。
- **平台规则依据**：`registry/versions.yaml` `nkb_schema: 1.3.0`；#231 §三 建议处置 1。
- **风险**：低（当前可运行）。
- **执行状态**：⏳ 保持 legacy；迁移需单独授权。

### ⏳ F6 平台 Core 文档疑似泄漏到项目根（待授权 + 逐份比对）
- **修改前问题**：项目根含 `AGENTS.md`(19KB)、`AI写作自动化流程.md`、`写作规则.md`(57KB)、`写作参考资料.md`(41KB)、`审查参考.txt`、`小说简介.md`、`时间线与人物关系图.md`、`大纲_1000章总体规划.md` 等。其中 `写作规则.md`/`AGENTS.md` 等疑似平台 Core 文档的项目内副本（`platform/core/` 已有对应 `写作规则`/`AGENTS.md`）。`README.md` §十 规定「小说仓库 docs/ 仅保留项目私有内容（治理索引 / 红线）」，核心文档应属 platform/。
- **修改后方案**：逐份比对平台 Core 同源文档，确认后——
  - 项目私有治理索引（如项目 `AGENTS.md`、`docs/governance/AUTHORITY_INDEX.md`）保留；
  - 纯 Core 副本移回引用、项目内删除或改为引用，避免复制 Core（三层隔离：Core 不可在项目内复制）。
- **平台规则依据**：`README.md` §十「全局 Core 文档已从小说仓库 docs/ 迁出；小说仓库 docs/ 仅保留项目私有内容」；三层隔离（Core 全局、Project 私有，Project 不得复制 Core 实现/文档）。
- **风险**：中（需逐份甄别，避免误删项目红线/私有治理内容）。
- **执行状态**：⏳ 待授权 + 逐份比对。

### 💡 F7 卷目录内正文与评审产物混放（软建议）
- **修改前问题**：`第一卷_道生/` 等同目录平铺章节正文（`第NNN章_*.md`）、审读评分卡（`审读_*.md`）、修复批注（`修复批注_*.md`）、`大纲/` 子目录。`canonical_manifest` 把评分卡也登记为 `published` 条目，混淆「canonical 正文」与「评审产物」。
- **修改后方案（软）**：评估将评审产物（`审读_*`/`修复批注_*`）迁入 `artifacts/`（当前仅含 README.md，空闲）或 `reviews/`，manifest 仅追踪正文；或维持现状（若平台约定如此）。属软优化，非阻断。
- **平台规则依据**：`project.yaml` `paths.artifacts: ./artifacts`（当前空闲）；`canonical_manifest` 追踪范围约定。
- **风险**：低（若调整需同步 manifest）。
- **执行状态**：💡 软建议，待授权。

### 💡 F8 总纲 vs 卷纲可能漂移（软建议）
- **修改前问题**：`project.yaml` `paths.outline: ./大纲_1000章总体规划.md`（总纲），而 `第一卷_道生/大纲/`、`第二卷_京华/大纲/` 为各卷大纲子目录。需确认总纲与卷纲一致、有无漂移。
- **修改后方案（软）**：比对总纲与卷纲，确保卷纲是总纲派生且一致；若一致保留（派生关系清晰），若漂移走大纲治理流。
- **平台规则依据**：`project.yaml` `paths.outline`；NKB/D 故事态一致性。
- **风险**：低。
- **执行状态**：💡 软建议，核查即可。

---

## 三、已执行项确认（F4）
- `tasks/archive/task_pilot_ch001_fix.yaml`（重复件归档）
- `tasks/claimed/task_fix_ch2_20_review.yaml`（真游离件恢复追踪）
- 根目录 `task_*.yaml` 已清空。内容未改动，可逆（如需可 mv 回根）。

## 四、下一步建议（待你裁定）
1. **立即可安全执行**：F2 方案(a)（在 `project.yaml` 增设 `paths.chapters_canonical` 澄清语义）——纯配置声明，低风险。
2. **需你授权后执行**：F1（生成/豁免 PROJECT_LAYOUT）、F6（文档去重比对）、F7/F8（软优化）。
3. **需平台工具 + 授权**：F3（txt/md 同步重生成）、F5（NKB 1.3.0 迁移）。
4. 建议补充一次 `platform doctor`（非 QuickMode）做内容深度体检，闭合 WARN。

> 说明：本项目 `task_system.direct_file_write: false` 且沿用 #231「待授权后再执行」惯例，故结构性/配置性改动均不擅自执行，待你确认后按平台受控通道（Task Packet → 受控写 → Gate → Publish）落地。
