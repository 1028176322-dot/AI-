schema_version: 1.2.0
project_id: novel-dsf
records: []
# 空 Schema 实例：由模板 xuanhuan（含 nkb-schema-extension）生成，待整合散落事实后填充。
# 每个对象须带 Version + Updated（见 NKB.md §2.1）。
# WorldState / Assets 由 Events 事件驱动派生（§2.2），不手填。
# Derived 由 K2 / K5 自动派生（§2.3），不手填。
